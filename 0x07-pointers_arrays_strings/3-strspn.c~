/**
 *_strspn - gets the length of a prefix substring
 *s - string containing substring
 *accept -
 *Return: number of bytes in prefix substring
*/

unsigned int _strspn(char *s, char *accept)
{

int i;
int j;
 for (i = 0 ; i != '\0' ; i++)
   {

     for (j = 0 ; 
   }






return ( );
}
