include "main.h"

/**
*_abs - computes absolute value of integer
*@n: The input number as an integer
*Return: always 0
*/
int _abs(int n)
{
if  (n >= 0)
{
return (n);
}
else if (n < 0)
{
return (n * -1);
}
}
