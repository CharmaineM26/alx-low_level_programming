#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
* mul - a function that checks for a digit
* @a: first integer to be computed
* @b: second integer to be computed
* Return: multiplication of two integers
*/
int mul(int a, int b);
{
	return  (a * b);
}
